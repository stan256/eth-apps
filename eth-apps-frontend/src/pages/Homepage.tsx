import React from 'react';
import Typography from "@mui/material/Typography";

const Homepage = () => {
    return (
        <Typography>One day there will be importent info here</Typography>
    );
};

export default Homepage;
